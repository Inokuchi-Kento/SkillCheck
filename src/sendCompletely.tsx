import './sendCompletely.css'

function sendCompletely() {

    return (
        <div className="sendCompletely">
            <h1>入力内容を送信しました。</h1>
            <button className='submitbutton' >メニュー画面へ</button>
        </div>
    )
}